@model Mwh.Sample.Domain.Models.EmployeeDto

@{
    ViewBag.Title = $"Delete Employee - {Model.Name} | ASP.NET MVC CRUD Tutorial";
    ViewBag.MetaDescription = $"Confirm deletion of employee {Model.Name}. Understand how the Employee Delete form integrates into the MVC Controller form approach in ASP.NET MVC.";
    ViewBag.MetaKeywords = "ASP.NET MVC, Employee Delete, CRUD tutorial, MVC Forms, employee management, form handling, model binding";
}

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-body">
            <h2 class="card-title text-center text-danger">Delete Employee - @Model.Name</h2>
            <h3 class="text-center text-muted">Are you sure you want to delete this employee?</h3>
            <div class="mt-4">
                <dl class="row">
                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Name)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Name)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Gender)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Gender)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Age)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Age)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.State)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.State)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Country)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Country)</dd>

                    <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Department)</dt>
                    <dd class="col-sm-9">@Html.DisplayFor(model => model.Department)</dd>
                </dl>
            </div>

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.Id)
                <div class="d-flex justify-content-center mt-4">
                    <input type="submit" value="Delete" class="btn btn-danger me-3" />
                    @Html.ActionLink("Back to List", "Index", null, htmlAttributes: new { @class = "btn btn-secondary" })
                </div>
            }
        </div>
    </div>
</div>

<div class="container mt-4">
    <div class="card shadow-sm">
        <div class="card-body">
            <h3 class="card-title">About the Employee Delete Form</h3>
            <p class="card-text">
                The Employee Delete form is a critical component of the MVC Controller form approach in ASP.NET MVC. It ensures secure and reliable deletion of employee records by using form validation and anti-forgery tokens.
                This form approach maintains data integrity and provides a user-friendly interface for managing employee records, with appropriate confirmation and feedback mechanisms.
            </p>
            <p class="card-text">
                This form is built using Bootstrap 5, making it visually appealing and responsive. The use of MVC architecture ensures a clean separation of concerns, supporting maintainable and scalable applications.
            </p>
        </div>
    </div>
</div>
